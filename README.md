# nsfw-api

[![Rust](https://github.com/maa123/nsfw-api/actions/workflows/rust.yml/badge.svg)](https://github.com/maa123/nsfw-api/actions/workflows/rust.yml)

## Configuration

Create a `config.toml` file in the root directory. You can use `config.example.toml` as a template:

```bash
cp config.example.toml config.toml
```

Edit `config.toml` to configure the server address and other settings.
